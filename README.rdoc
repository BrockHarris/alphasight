== Sample App for AlphaSights
All email prediction logic is handled in the model <tt>advisor.rb</tt> which uses self-explanatory class/method naming, and markup where needed.


== Setup

1. Install the needed gems:
       bundle

2. Setup & populate the database with sample data:
       rake db:setup

3. Navigate to localhost:3000 to see the original data and predicted results.